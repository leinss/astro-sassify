---
import { getCollection } from "astro:content"
import Layout from "../layouts/Layout.astro"
import Header from "./Header.astro"
import Footer from "./Footer.astro"
import BackToTop from "./BackToTop.astro"
import BlogCard from "./BlogCard.astro"
import ProjectCard from "./ProjectCard.astro"
import i18n from "../assets/i18n.json"
import config from "../assets/config.ts"
import { blogImages } from "../assets/blogImages.ts"

interface Props {
  lang: "en" | "de"
}

const { lang } = Astro.props
const t = i18n[lang]
const p = t.portfolio

const basePath = lang === "en" ? "/en" : "/de"
const alternateHref = lang === "en" ? "/de/projekte/" : "/en/projects/"
const calUrl =
  lang === "en"
    ? config.external_links.strategy_call_en
    : config.external_links.strategy_call_de

interface ProjectContent {
  title: string
  tagline: string
  problem: string
  solution: string
  result: string
  tags: string[]
}

const pc = p.projects as Record<
  "invoice" | "lead" | "meeting" | "excel" | "faq",
  ProjectContent
>

// Non-translatable wiring: optimized image + the blog-post slug (per language)
// whose page hosts the live demo widget.
const projects = [
  {
    image: blogImages["/images/blog/invoice-automation.png"],
    slug: lang === "en" ? "automating-invoice-processing" : "rechnungsverarbeitung-automatisieren",
    content: pc.invoice,
    workflow: "n8n-invoice-cloud.json",
  },
  {
    image: blogImages["/images/blog/communication-automation.png"],
    slug: lang === "en" ? "automating-communication" : "kommunikation-automatisieren-menschlich-bleiben",
    content: pc.lead,
    workflow: "blitz-antwort.json",
  },
  {
    image: blogImages["/images/blog/meeting-automation.png"],
    slug: lang === "en" ? "meeting-minutes-automation" : "meeting-protokoll-automatisieren",
    content: pc.meeting,
    workflow: "meeting-protokoll.json",
  },
  {
    image: blogImages["/images/blog/from-spreadsheets-to-systems.png"],
    slug: lang === "en" ? "from-spreadsheets-to-systems" : "von-tabellen-zu-systemen",
    content: pc.excel,
    workflow: "excel-retter.json",
  },
  {
    image: blogImages["/images/blog/faq-automation.png"],
    slug: lang === "en" ? "faq-automation-ai-support" : "faq-assistent-ki-support",
    content: pc.faq,
    workflow: "faq-assistent.json",
  },
]

// Case-study posts rendered with the standard BlogCard.
const caseStudies = (
  await getCollection(
    "blog",
    ({ data }) => data.lang === lang && !data.draft && data.category === "reference-build",
  )
).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
---

<Layout
  title={p.meta_title}
  description={p.meta_description}
  lang={lang}
  alternateUrl={alternateHref}
>
  <Header lang={lang} alternateHref={alternateHref} />

  <main class="min-h-screen bg-linear-to-b from-primary-50 to-white dark:from-secondary-950 dark:to-secondary-900">
    <!-- Hero -->
    <section class="py-16 md:py-24">
      <div class="container-custom">
        <div class="max-w-3xl mx-auto text-center">
          <h1 class="text-4xl md:text-5xl font-bold text-secondary-900 dark:text-white mb-6">
            {p.title}
          </h1>
          <p class="text-xl text-secondary-700 dark:text-secondary-400">
            {p.subtitle}
          </p>
        </div>
      </div>
    </section>

    <!-- Live demos -->
    <section class="pb-12">
      <div class="container-custom">
        <div class="max-w-3xl mb-10">
          <h2 class="text-2xl md:text-3xl font-bold text-secondary-900 dark:text-white mb-3">
            {p.demos_title}
          </h2>
          <p class="text-secondary-700 dark:text-secondary-400">
            {p.demos_subtitle}
          </p>
        </div>
        <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
          {projects.map((proj) => (
            <ProjectCard
              lang={lang}
              image={proj.image}
              href={`${basePath}/blog/${proj.slug}/`}
              title={proj.content.title}
              tagline={proj.content.tagline}
              problem={proj.content.problem}
              solution={proj.content.solution}
              result={proj.content.result}
              tags={proj.content.tags}
            />
          ))}
        </div>
      </div>
    </section>

    <!-- Download the real workflows (proof) -->
    <section class="pb-16">
      <div class="container-custom">
        <div class="max-w-4xl mx-auto bg-white dark:bg-secondary-800/50 border border-secondary-200 dark:border-secondary-700 rounded-2xl p-8 md:p-10">
          <h2 class="text-2xl md:text-3xl font-bold text-secondary-900 dark:text-white mb-3">
            {p.workflows_title}
          </h2>
          <p class="text-secondary-700 dark:text-secondary-400 mb-8 max-w-3xl">
            {p.workflows_subtitle}
          </p>
          <ul class="grid gap-3 sm:grid-cols-2">
            {projects.map((proj) => (
              <li>
                <a
                  href={`/workflows/${proj.workflow}`}
                  download
                  class="group flex items-center justify-between gap-3 rounded-lg border border-secondary-200 dark:border-secondary-700 bg-secondary-50/60 dark:bg-secondary-900/40 px-4 py-3 hover:border-primary-400 dark:hover:border-primary-500 transition-colors"
                >
                  <span class="min-w-0">
                    <span class="block font-medium text-secondary-900 dark:text-white truncate">{proj.content.title}</span>
                    <span class="block text-xs font-mono text-secondary-700 dark:text-secondary-400 truncate">{proj.workflow}</span>
                  </span>
                  <span class="shrink-0 inline-flex items-center gap-1 text-sm font-medium text-primary-700 dark:text-primary-400">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v2a2 2 0 002 2h12a2 2 0 002-2v-2M7 10l5 5m0 0l5-5m-5 5V3" />
                    </svg>
                    <span class="hidden sm:inline">{p.workflows_cta}</span>
                  </span>
                </a>
              </li>
            ))}
          </ul>
          <p class="mt-6 text-sm text-secondary-700 dark:text-secondary-400">
            {p.workflows_note}
          </p>
        </div>
      </div>
    </section>

    <!-- Reference builds -->
    {caseStudies.length > 0 && (
      <section class="pb-16">
        <div class="container-custom">
          <div class="max-w-3xl mb-10">
            <h2 class="text-2xl md:text-3xl font-bold text-secondary-900 dark:text-white mb-3">
              {p.case_studies_title}
            </h2>
            <p class="text-secondary-700 dark:text-secondary-400">
              {p.case_studies_subtitle}
            </p>
          </div>
          <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-4">
            {caseStudies.map((post) => <BlogCard post={post} lang={lang} />)}
          </div>
        </div>
      </section>
    )}

    <!-- Closing CTA -->
    <section class="pb-24">
      <div class="container-custom">
        <div class="max-w-4xl mx-auto text-center bg-white dark:bg-secondary-800/50 border border-secondary-200 dark:border-secondary-700 rounded-2xl p-8 md:p-12">
          <h2 class="text-2xl md:text-3xl font-bold text-secondary-900 dark:text-white mb-4">
            {p.cta_title}
          </h2>
          <p class="text-secondary-700 dark:text-secondary-400 mb-8 max-w-2xl mx-auto">
            {p.cta_subtitle}
          </p>
          <a
            href={calUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="btn-primary inline-flex items-center"
          >
            {p.cta_button}
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </div>
      </div>
    </section>
  </main>

  <Footer lang={lang} />
  <BackToTop />
</Layout>
