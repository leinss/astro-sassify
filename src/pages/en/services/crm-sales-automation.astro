---
export const prerender = true;

import Layout from '../../../layouts/Layout.astro';
import Header from '../../../components/Header.astro';
import ServicePage from '../../../components/ServicePage.astro';
import Footer from '../../../components/Footer.astro';
import BackToTop from '../../../components/BackToTop.astro';
import i18n from '../../../assets/i18n.json';

const lang = 'en';
const pillar = 'crm';
const t = i18n[lang].services[pillar];
const alternateHref = `/de/services/${i18n.de.services[pillar].slug}/`;
---

<Layout
  title={`${t.meta_title} - Tobias Leinss`}
  description={t.meta_description}
  lang={lang}
  alternateUrl={alternateHref}
>
  <Header lang={lang} alternateHref={alternateHref} />
  <main>
    <ServicePage lang={lang} pillar={pillar} />
  </main>
  <Footer lang={lang} />
  <BackToTop />
</Layout>
